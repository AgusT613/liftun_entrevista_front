import type { IRoutes } from '@/types'
import { FaSquareArrowUpRight } from 'react-icons/fa6'

export const routes: IRoutes[] = [
  {
    section: 'Create Action',
    url: '/create-action',
    icon: <FaSquareArrowUpRight className="text-xl" />
  }
]
